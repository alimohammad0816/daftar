# دفتر — راهنمای کار

دفترچهٔ یادداشت روزانهٔ شخصی با تقویم شمسی، کارهای روزانه و ادیتور متن غنی.
طرح کامل در `PLAN.md`. **قبل از هر کاری آن را بخوان.**

## ساختار

```
web/    Next.js 15 (App Router) + MUI v7 + TipTap + Yjs
api/    FastAPI + SQLite (venv با uv، جزئیات در api/README.md)
```

## روش کار

- **یک فاز در هر بار.** فازها در بند ۱۵.۴ هستند. تا وقتی فاز جاری کامل و کار نکند، سراغ بعدی نرو.
- هر فاز = یک commit. پیام commit با شمارهٔ فاز شروع شود.
- اگر چیزی در `PLAN.md` مبهم بود، بپرس. حدس نزن.
- **کد اضافه ننویس.** قابلیتی که در فاز جاری نیست، ساخته نمی‌شود.
- بستهٔ `web` با **yarn** مدیریت می‌شود (نه pnpm/npm).
- بستهٔ `api` با `uv venv` + `requirements.txt` مدیریت می‌شود؛ بدون ORM (بند ۱۶.۲).

## قواعدی که هرگز نقض نمی‌شوند

1. **حذف = tombstone.** حذف یادداشت هرگز با پاک کردن `Y.Doc` انجام نمی‌شود، بلکه با علامت‌گذاری در سند `index`. از فاز ۲، حتی وقتی سروری وجود ندارد.
2. **سرور محتوا را باز نمی‌کند.** بسته‌های Yjs به‌عنوان `BLOB` مبهم ذخیره می‌شوند. بدون FTS5 روی متن یادداشت، بدون رندر سمت سرور، بدون تولید PDF روی سرور. (بند ۱۴.۴ — این باعث می‌شود رمزنگاری در آینده ارزان بماند.)
3. **جست‌وجو سمت کلاینت است.** همیشه.
4. **ورود دروازهٔ کل اپ است** (بازنگری فاز ۵، بند ۱۴.۱). پیش از نشست معتبر هیچ صفحه‌ای — نه فقط تنظیمات — رندر نمی‌شود؛ `AuthGate` در `app/layout.js` این را تضمین می‌کند.
5. **`--workers 1` برای uvicorn.** پخش پیام از حافظه است؛ چند worker آن را بی‌صدا می‌شکند.

## قواعد رابط کاربری

- `dir="rtl"` و `lang="fa"` روی `<html>`؛ کش Emotion با `stylis-plugin-rtl`.
- فونت وزیرمتن با `next/font/local` از `public/fonts`. به Google Fonts وصل نشو.
- **همهٔ اعداد فارسی.** هلپر `toFa()` بنویس و همه‌جا استفاده کن.
- **موبایل مرجع طراحی است**، دسکتاپ گسترش آن. هدف لمس حداقل ۴۴ پیکسل.
- دو پالت در `theme/theme.js`: `azure` (پیش‌فرض، از تم landing پروژهٔ beeplan — بند ۱۰ در PLAN.md) و `turquoise` (از `beehoosh-web/src/theme/tokens.js`). کاربر در تنظیمات انتخاب می‌کند؛ هر پالت نسخهٔ روشن و تیرهٔ خودش را دارد. نام توکن‌ها در هر دو یکی است، پس هیچ کامپوننتی نباید بداند کدام پالت فعال است — رنگ را از `theme.palette` بگیر، نه ثابتِ دستی. قرمز `#B3261E` **فقط** برای تعطیلات. جای دیگری خرج نشود.
- جزیره‌های LTR (کد، لینک، نام فایل انگلیسی) با `unicode-bidi: isolate` جدا شوند.

## دام‌های شناخته‌شده

- بلوک کد باید `dir="ltr"` اجباری داشته باشد (کد فارسی‌چین ناخواناست). جدول برخلاف این، `dir="rtl"` می‌گیرد تا ستون اول راست باشد (تصمیم فاز ۶ — بند ۴ PLAN.md؛ نسخهٔ قبلی این خط جدول را هم اشتباهاً جزو ltr می‌گفت).
- تولبار ادیتور روی iOS با `position: fixed` زیر کیبورد گم می‌شود — از `window.visualViewport` استفاده کن.
- TOTP فقط `SHA1` و ۶ رقم و دورهٔ ۳۰ ثانیه. Google Authenticator بقیه را بی‌صدا اشتباه می‌خواند.
- تعطیلات قمری محاسبه‌پذیر نیستند؛ از JSON سالانه بیایند (بند ۵).
- تصویر قبل از ذخیره فشرده شود: حداکثر ۱۶۰۰px، WebP. اما همان عدد را اندازهٔ *نمایش* نگیر — قاب نمایش جدا محدود می‌شود (`AttachmentImageView`)، وگرنه عکس تمام پهنای ادیتور را می‌گیرد.
- `input[type=file]` تصویر **نباید** `capture` بگیرد؛ با آن گوشی مستقیم دوربین را باز می‌کند و گالری بی‌راه می‌ماند.
- ورودی‌ای که با `onBlur` ثبت می‌کند باید `document.hasFocus()` را چک کند: عوض‌کردن زبان صفحه‌کلید (Win+Space) فوکوس پنجره را می‌گیرد و همان blur را می‌فرستد — متن نیمه‌کاره ثبت می‌شود.
- هر صفحه‌ای که سندی را نشان می‌دهد باید `useLiveSync` خودش را داشته باشد؛ IndexedDB بدون آن ساکت و محلی کار می‌کند و «همگام نشدن» دیر معلوم می‌شود.

## وضعیت

- [x] فاز ۰ — اسکلت
- [x] فاز ۱ — تقویم و تعطیلات
- [x] فاز ۲ — Yjs + y-indexeddb + کارهای روزانه
- [x] فاز ۳ — ادیتور TipTap
- [x] فاز ۴ — FastAPI + ورود با رمز و TOTP
- [x] فاز ۵ — رلهٔ WebSocket + snapshot (+ بازنگری قاعدهٔ ۴: ورود دروازهٔ کل اپ شد)
- [x] فاز ۶ — تصویر، ضمیمه، کد، جدول
- [x] فاز ۷ — یادداشت آزاد، جست‌وجو (تگ عمداً کنار گذاشته شد — درخواست کاربر برای نسخهٔ اولیهٔ ساده‌تر؛ بعداً اضافه می‌شود)

بعد از اتمام هر فاز، تیک آن را اینجا بزن.
