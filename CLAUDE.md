# دفتر — راهنمای کار

دفترچهٔ یادداشت روزانهٔ شخصی با تقویم شمسی، کارهای روزانه و ادیتور متن غنی.
طرح کامل در `PLAN.md`. **قبل از هر کاری آن را بخوان.**

## ساختار

```
web/    Next.js 15 (App Router) + MUI v7 + TipTap + Yjs
api/    FastAPI + SQLite   ← تا فاز ۴ خالی می‌ماند، دست نزن
```

## روش کار

- **یک فاز در هر بار.** فازها در بند ۱۵.۴ هستند. تا وقتی فاز جاری کامل و کار نکند، سراغ بعدی نرو.
- هر فاز = یک commit. پیام commit با شمارهٔ فاز شروع شود.
- اگر چیزی در `PLAN.md` مبهم بود، بپرس. حدس نزن.
- **کد اضافه ننویس.** قابلیتی که در فاز جاری نیست، ساخته نمی‌شود.

## قواعدی که هرگز نقض نمی‌شوند

1. **حذف = tombstone.** حذف یادداشت هرگز با پاک کردن `Y.Doc` انجام نمی‌شود، بلکه با علامت‌گذاری در سند `index`. از فاز ۲، حتی وقتی سروری وجود ندارد.
2. **سرور محتوا را باز نمی‌کند.** بسته‌های Yjs به‌عنوان `BLOB` مبهم ذخیره می‌شوند. بدون FTS5 روی متن یادداشت، بدون رندر سمت سرور، بدون تولید PDF روی سرور. (بند ۱۴.۴ — این باعث می‌شود رمزنگاری در آینده ارزان بماند.)
3. **جست‌وجو سمت کلاینت است.** همیشه.
4. **ورود دروازهٔ همگام‌سازی است، نه دروازهٔ اپ.** اپ همیشه با دادهٔ محلی باز می‌شود. (بند ۱۴.۱)
5. **`--workers 1` برای uvicorn.** پخش پیام از حافظه است؛ چند worker آن را بی‌صدا می‌شکند.

## قواعد رابط کاربری

- `dir="rtl"` و `lang="fa"` روی `<html>`؛ کش Emotion با `stylis-plugin-rtl`.
- فونت وزیرمتن با `next/font/local` از `public/fonts`. به Google Fonts وصل نشو.
- **همهٔ اعداد فارسی.** هلپر `toFa()` بنویس و همه‌جا استفاده کن.
- **موبایل مرجع طراحی است**، دسکتاپ گسترش آن. هدف لمس حداقل ۴۴ پیکسل.
- قرمز `#B3261E` **فقط** برای تعطیلات. جای دیگری خرج نشود.
- جزیره‌های LTR (کد، لینک، نام فایل انگلیسی) با `unicode-bidi: isolate` جدا شوند.

## دام‌های شناخته‌شده

- بلوک کد و جدول باید `dir="ltr"` اجباری داشته باشند.
- تولبار ادیتور روی iOS با `position: fixed` زیر کیبورد گم می‌شود — از `window.visualViewport` استفاده کن.
- TOTP فقط `SHA1` و ۶ رقم و دورهٔ ۳۰ ثانیه. Google Authenticator بقیه را بی‌صدا اشتباه می‌خواند.
- تعطیلات قمری محاسبه‌پذیر نیستند؛ از JSON سالانه بیایند (بند ۵).
- تصویر قبل از ذخیره فشرده شود: حداکثر ۱۶۰۰px، WebP.

## وضعیت

- [ ] فاز ۰ — اسکلت
- [ ] فاز ۱ — تقویم و تعطیلات
- [ ] فاز ۲ — Yjs + y-indexeddb + کارهای روزانه
- [ ] فاز ۳ — ادیتور TipTap
- [ ] فاز ۴ — FastAPI + ورود با رمز و TOTP
- [ ] فاز ۵ — رلهٔ WebSocket + snapshot
- [ ] فاز ۶ — تصویر، ضمیمه، کد، جدول
- [ ] فاز ۷ — یادداشت آزاد، تگ، جست‌وجو

بعد از اتمام هر فاز، تیک آن را اینجا بزن.
