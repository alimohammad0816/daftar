# دفتر — طرح فنی

دفترچهٔ یادداشت روزانه با تقویم شمسی، فهرست کار روزانه و ادیتور متن غنی.
تک‌کاربره، self-hosted، فارسی و RTL، **موبایل‌محور**.

---

## ۰. سه تصمیمی که همه‌چیز را شکل می‌دهند

1. **گوشی ابزار اصلی است.** پس موبایل مرجع طراحی است و دسکتاپ گسترش آن، نه برعکس. PWA و آفلاین از الزامات فاز اول‌اند نه پرداخت آخر کار.
2. **یادداشت روزانه و یادداشت آزاد، هر دو.** یعنی `dayKey` اختیاری است و یادداشت‌ها به تگ نیاز دارند نه فقط تاریخ.
3. **تصویر، ضمیمه، بلوک کد و جدول.** این یعنی لایهٔ ذخیره‌سازی باید باینری نگه دارد، و همگام‌سازی فاز آخر به‌مراتب سنگین‌تر از حالت فقط‌متن است.

---

## ۱. استک

| موضوع | انتخاب | چرا |
|---|---|---|
| فریم‌ورک | Next.js 15، App Router | خواستهٔ خودت + امکان API در همان پروژه |
| UI | MUI v7 + Emotion | خواستهٔ خودت؛ RTL آماده دارد |
| ادیتور | TipTap 3 | خروجی JSON ساختاریافته، جدول و کد و نود سفارشی دارد |
| تاریخ | `date-fns-jalali` + `AdapterDateFnsJalaliV3` | با MUI X Date Pickers سازگار است |
| ذخیره‌سازی محلی | Dexie روی IndexedDB (شامل بلاب فایل‌ها) | آفلاین، سریع، بدون وابستگی به شبکه |
| PWA | `@serwist/next` | نگهداری‌شده‌تر از next-pwa |
| بک‌اند | **FastAPI (Python)** + SQLite + دیسک برای فایل‌ها | سرویس جدا از Next.js؛ جزئیات در بخش ۱۶ |
| احراز هویت | نام کاربری + رمز عبور + TOTP (Google Authenticator) | ساده، بدون وابستگی بیرونی، کافی برای اپ تک‌کاربره (بخش ۱۴) |
| رمزنگاری سرتاسری | **به تعویق افتاده** — بخش ۱۴.۴ | اول راه‌اندازی و استفاده؛ افزودنش بعداً ارزان است اگر سه قاعده رعایت شود |

> **چرا SQLite و نه Mongo؟** اپ تک‌کاربره است و بار نوشتنش ناچیز. مزیت اصلی SQLite اینجا **FTS5** است که جست‌وجوی تمام‌متن را بدون سرویس اضافه می‌دهد. اگر می‌خواهی روی استک فعلی خودت بماند، FastAPI + Mongo هم کار می‌کند ولی برای جست‌وجو یا به `$text` بسنده می‌کنی یا Meilisearch اضافه می‌شود.

> **local-first قابل مذاکره نیست.** روی گوشی و شبکهٔ ناپایدار، ادیتور نباید حتی یک لحظه منتظر شبکه بماند. نوشتن همیشه در IndexedDB، همگام‌سازی در پس‌زمینه.

---

## ۲. چیدمان موبایل

روی گوشی شبکهٔ ماه جای زیادی می‌گیرد و ادیتور را خفه می‌کند. راه‌حل: **نوار هفته** به‌صورت پیش‌فرض، شبکهٔ ماه فقط وقتی خواستی.

```
┌──────────────────────────────┐
│  ☰   مرداد ۱۴۰۵      ▦  ⋯   │  ← ضربه روی ▦ شبکهٔ ماه را در BottomSheet باز می‌کند
├──────────────────────────────┤
│  ج   پ   چ   س   د   ی   ش  │  ← نوار هفته، افقی، سوایپ برای هفتهٔ قبل/بعد
│ ۲۹  ۲۸  ۲۷ (۲۶) ۲۵  ۲۴  ۲۳  │     نقطهٔ زیر هر روز = یادداشت/کار دارد
├──────────────────────────────┤
│ ▾ کارها               ۲/۳   │  ← جمع‌شونده، پیش‌فرض باز اگر کار باز دارد
│   ☑ بازبینی pull request     │
│   ☐ تماس با تیم فروش         │
│   ＋ کار تازه…               │
├──────────────────────────────┤
│                              │
│   متن یادداشت…               │  ← بیشترین فضا مال ادیتور
│                              │
├──────────────────────────────┤
│  B  I  •  ▦  <>  📷  ⋯      │  ← تولبار چسبیده بالای کیبورد
├──────────────────────────────┤
│   روز    یادداشت‌ها   کارها   │  ← BottomNavigation
└──────────────────────────────┘
```

نکات اجرایی که وقت می‌گیرند:

- **تولبار روی کیبورد.** روی iOS Safari با `position: fixed` تولبار زیر کیبورد گم می‌شود. باید از `window.visualViewport` (رویدادهای `resize` و `scroll`) استفاده کنی و موقعیت تولبار را دستی حساب کنی. این را در فاز ۳ جدی بگیر، وصله‌کردنش بعداً سخت است.
- **سوایپ افقی برای رفتن به روز بعد/قبل.** حس سررسید می‌دهد و ناوبری را از دکمه بی‌نیاز می‌کند.
- **هدف لمس حداقل ۴۴ پیکسل.** خانه‌های تقویم و چک‌باکس‌ها در پروتوتایپ دسکتاپ کوچک‌ترند.
- **تست روی گوشی واقعی از فاز ۲.** Service Worker به HTTPS نیاز دارد؛ با `mkcert` روی شبکهٔ محلی گواهی بساز، شبیه‌ساز کافی نیست.

دسکتاپ همان چیدمان سه‌ستونی پروتوتایپ می‌ماند؛ در `md` به بالا شبکهٔ ماه دائمی می‌شود و نوار هفته کنار می‌رود.

---

## ۳. مدل داده

```ts
type DayKey = string;  // تاریخ شمسی: "1405-05-26"

interface Note {
  id: string;
  kind: 'daily' | 'free';
  dayKey: DayKey | null;   // برای free برابر null
  title: string;           // برای daily خودکار از تاریخ، قابل بازنویسی
  content: JSONContent;    // خروجی TipTap
  plain: string;           // متن خام، فقط برای جست‌وجو
  tags: string[];
  pinned: boolean;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null; // حذف نرم، برای همگام‌سازی لازم است
}

interface Task {
  id: string;
  dayKey: DayKey;
  title: string;
  done: boolean;
  doneAt: string | null;
  order: number;            // ترتیب دستی
  priority: 0 | 1 | 2;
  rollover: boolean;        // اگر انجام نشد، خودکار برود روز بعد
  noteId: string | null;
}

interface Attachment {
  id: string;
  noteId: string;
  kind: 'image' | 'file';
  name: string;
  mime: string;
  size: number;
  width?: number;           // برای تصویر، تا قبل از لود جای خالی رزرو شود
  height?: number;
  blob: Blob | null;        // محلی
  remoteUrl: string | null; // بعد از همگام‌سازی
  createdAt: string;
}
```

جدول‌های Dexie: `notes`, `tasks`, `attachments`, `meta`
ایندکس‌ها: `tasks(dayKey, order)`، `notes(dayKey)`، `notes(updatedAt)`، `notes(*tags)`، `attachments(noteId)`

---

## ۴. ادیتور

```bash
yarn add @tiptap/react @tiptap/starter-kit @tiptap/extension-underline \
  @tiptap/extension-text-align @tiptap/extension-highlight @tiptap/extension-link \
  @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell \
  @tiptap/extension-table-header @tiptap/extension-image @tiptap/extension-placeholder \
  @tiptap/extension-character-count @tiptap/extension-code-block-lowlight lowlight
```

افزونه‌های سفارشی که باید بنویسی:

- **`Direction`** — attribute `dir` روی هر بلوک می‌گذارد. بدون این، پاراگرافی که با کلمهٔ انگلیسی شروع شود چپ‌چین می‌پرد. دکمهٔ راست‌چین/چپ‌چین در تولبار همین را عوض کند.
- **`FileAttachment`** — یک atom node با NodeView که آیکن و نام فایل و حجم را نشان دهد و `attachmentId` نگه دارد. خودِ فایل هیچ‌وقت داخل JSON نرود.

نکات هر کدام از چهار قابلیتی که خواستی:

- **تصویر:** قبل از ذخیره حتماً سمت کلاینت فشرده کن (حداکثر ضلع ۱۶۰۰ پیکسل، کیفیت ۰٫۸، خروجی WebP). عکس خام گوشی چهار مگابایت است و بیست‌تا از آن سهمیهٔ IndexedDB را می‌بلعد. روی موبایل `<input type="file" accept="image/*" capture="environment">` دوربین را مستقیم باز می‌کند. Paste و Drag & Drop را هم برای دسکتاپ وصل کن. در سند فقط `attachmentId` ذخیره شود و NodeView با `URL.createObjectURL` بلاب را نشان دهد — و در `useEffect` حتماً `revokeObjectURL` صدا بزن وگرنه حافظه نشت می‌کند.
- **بلوک کد:** `CodeBlockLowlight` با زبان‌های محدود (js, ts, python, bash, sql, json). داخلش **اجباراً** `dir="ltr"` و فونت monospace، وگرنه کد فارسی‌چین می‌شود و ناخواناست.
- **جدول:** روی موبایل دردسر است. جدول را در یک wrapper با `overflow-x: auto` بگذار و کنترل‌های افزودن/حذف سطر و ستون را به‌جای منوی شناور، در یک BottomSheet بگذار که با انتخاب جدول باز شود. جدول هم `dir="rtl"` بگیرد تا ستون اول سمت راست باشد.
- **ضمیمه:** فقط نمایش کارت فایل و دانلود. پیش‌نمایش PDF داخل اپ را فعلاً نساز، ارزشش را ندارد.

autosave با debounce ۶۰۰ میلی‌ثانیه، به‌علاوهٔ ذخیرهٔ اجباری روی `blur` و روی `visibilitychange` — روی موبایل کاربر وسط تایپ اپ را می‌بندد.

---

## ۵. تعطیلات — مهم‌ترین نکتهٔ داده‌ای پروژه

دو دسته‌اند:

- **خورشیدی ثابت** (نوروز، ۱۳ فروردین، ۱۴ و ۱۵ خرداد، ۲۲ بهمن، ۲۹ اسفند…) — قابل هاردکد، در پروتوتایپ همین کار شده.
- **قمری متغیر** (اعیاد و وفیات) — هر سال جابه‌جا می‌شوند و گاهی با اعلام دولت وسط سال تغییر می‌کنند. محاسبه‌پذیر نیستند، باید از دیتاست بیایند.

منابع:

1. `samanzamani/PersianHoliday` — دیتاست استخراج‌شده از time.ir با پوشش ۱۳۷۰ تا ۱۴۳۰ و فایل SQLite آماده. **پیشنهاد اول.**
2. `BaseMax/persian-holidays-api` — اسکریپت پایتون که از time.ir می‌گیرد و JSON با فلگ `is_holiday` می‌دهد.
3. `pnldev.com/api/calender` — وب‌سرویس رایگان بدون کلید. برای seed خوب است، برای زمان اجرا نه.

**روش:** یک بار دیتا را بگیر، در `data/holidays/1404.json`, `1405.json`… ذخیره کن، و اسکریپت `yarn holidays:sync <year>` بنویس که سالی یک‌بار دستی اجرا شود. اپ در زمان اجرا هرگز به اینترنت وصل نشود — روی گوشی و آفلاین باید کار کند.

---

## ۶. ماندگاری داده روی موبایل — تلهٔ اصلی

- در بوت اپ `navigator.storage.persist()` را صدا بزن و نتیجه را در تنظیمات نشان بده.
- **روی iOS، اگر PWA به صفحهٔ اصلی اضافه نشده باشد، Safari می‌تواند IndexedDB را بعد از حدود هفت روز بی‌استفادگی پاک کند.** در اولین اجرا روی iOS یک راهنمای «افزودن به صفحهٔ اصلی» نشان بده. این یک هشدار تزئینی نیست؛ بدون آن ممکن است یادداشت‌هایت را از دست بدهی.
- **بکاپ دستی از فاز ۲**، نه فاز آخر: خروجی ZIP شامل `data.json` و پوشهٔ `attachments/`. تا قبل از راه‌افتادن سرور، این تنها تور نجات توست.
- در تنظیمات مصرف فضا را نشان بده (`navigator.storage.estimate()`).

---

## ۷. ساختار پوشه

```
app/
  layout.tsx                 # dir="rtl" lang="fa" + ThemeRegistry + ثبت SW
  (main)/page.tsx            # نمای روز
  (main)/notes/page.tsx      # یادداشت‌ها (روزانه + آزاد) با تگ و جست‌وجو
  (main)/notes/[id]/page.tsx
  (main)/tasks/page.tsx
  (main)/settings/page.tsx   # بکاپ، فضا، تم
  api/…                      # فاز ۶
components/
  layout/{TopBar,BottomNav,NavRail}.tsx
  calendar/{WeekStrip,MonthGrid,MonthSheet,DayCell}.tsx
  tasks/{TaskList,TaskItem,TaskInput}.tsx
  editor/
    Editor.tsx
    Toolbar.tsx              # به‌علاوهٔ MobileToolbar با visualViewport
    extensions/{Direction.ts,FileAttachment.tsx}
    nodeviews/{ImageView.tsx,FileView.tsx}
lib/
  jalali.ts                  # wrapper نازک روی date-fns-jalali
  holidays.ts                # لود JSON + getEvent(dayKey)
  media.ts                   # فشرده‌سازی تصویر
  db/{dexie.ts,repo.ts,backup.ts}   # UI مستقیم با Dexie حرف نزند
theme/{theme.ts,rtl-cache.ts}
data/holidays/*.json
```

---

## ۸. RTL و فونت

```bash
yarn add @emotion/cache stylis stylis-plugin-rtl
```

```ts
const rtlCache = createCache({ key: 'muirtl', stylisPlugins: [prefixer, rtlPlugin] });
// theme: { direction: 'rtl' }   |   <html dir="rtl" lang="fa">
```

- فونت **وزیرمتن** با `next/font/local` از `public/fonts`. به Google Fonts وابسته نشو — اپ باید آفلاین کامل باشد.
- هلپر `toFa(n)` بنویس و همه‌جا استفاده کن.
- جزیره‌های LTR (کد، لینک، نام فایل انگلیسی) را با کلاس `.ltr { direction: ltr; text-align: left; unicode-bidi: isolate; }` جدا کن.

---

## ۹. فازبندی برای Claude Code

هر فاز یک commit مستقل و قابل اجرا. ترتیب نسبت به نسخهٔ اول عوض شده چون موبایل اولویت دارد.

- **فاز ۰ — اسکلت:** Next.js + MUI + کش RTL + وزیرمتن + تم روشن/تیره + BottomNavigation در موبایل و NavRail در دسکتاپ. خروجی: پوستهٔ خالی که روی گوشی درست است.
- **فاز ۱ — تقویم:** `lib/jalali.ts`، `WeekStrip`، `MonthSheet`، `MonthGrid` دسکتاپ، سوایپ بین روزها، لود JSON تعطیلات، قرمزکردن تعطیل. خروجی: تقویم کارا بدون داده.
- **فاز ۲ — داده و کارها و PWA:** Dexie، CRUD تسک، تیک، ترتیب دستی، rollover، Serwist و آفلاین، `storage.persist()`، بکاپ ZIP دستی. خروجی: روی گوشی نصب می‌شود و بدون اینترنت کار می‌کند.
- **فاز ۳ — ادیتور پایه:** TipTap با متن، عنوان، فهرست، نقل‌قول، هایلایت، `Direction`، تولبار موبایل روی کیبورد، autosave. خروجی: نوشتن روان روی گوشی.
- **فاز ۴ — محتوای غنی:** تصویر با فشرده‌سازی، ضمیمهٔ فایل، بلوک کد، جدول. خروجی: چهار قابلیتی که خواستی.
- **فاز ۵ — یادداشت آزاد و جست‌وجو:** `kind: 'free'`، تگ، پین، صفحهٔ یادداشت‌ها، جست‌وجوی محلی روی `plain`.
- **فاز ۶ — سرور:** *(این فازبندی با بند ۱۳.۸ جایگزین شده است — به آن مراجعه کن)*

**فاز ۶ را دست‌کم نگیر.** همگام‌سازی دوطرفه با فایل باینری، حذف نرم و تعارض ویرایش، به‌تنهایی به اندازهٔ فازهای صفر تا سه کار دارد. اگر عجله داری، می‌شود موقتاً به «بکاپ خودکار به یک اندپوینت» بسنده کرد و همگام‌سازی واقعی را بعداً ساخت.

---

## ۱۰. نگاشت پروتوتایپ به MUI

| در پروتوتایپ | در پروژهٔ واقعی |
|---|---|
| `.topbar` | `<AppBar position="sticky" elevation={0}>` |
| `.rail` | `<Drawer variant="permanent">` در دسکتاپ، `<BottomNavigation>` در موبایل |
| `.cell` | `<ButtonBase>` با `sx` |
| `.box` | `<Checkbox>` |
| `.chip` | `<Chip size="small">` |
| `.bar` | `<LinearProgress variant="determinate">` |
| `.card` | `<Card><CardActionArea>` |
| `.tb` | `<ToggleButtonGroup>` با `editor.isActive()` برای حالت روشن |
| شبکهٔ ماه در موبایل | `<SwipeableDrawer anchor="bottom">` |
| رنگ‌های CSS variable | `theme.palette` — `holiday` را رنگ سفارشی تم کن |

پالت: برگرفته از تم landing پروژهٔ beeplan (`web/src/app/landing`، به‌خصوص `components/preview/tokens.js`) — آبی لاجوردی به‌عنوان رنگ اصلی و فیروزه‌ای به‌عنوان رنگ دوم، هرکدام با یک نسخهٔ روشن و یک نسخهٔ تیره:

| | روشن | تیره |
|---|---|---|
| اصلی (آبی لاجوردی) | `#1B6EC4` | `#5AA6F0` |
| دوم (فیروزه‌ای) | `#0D9488` | `#6FE0D2` |
| پس‌زمینه | `#F4F7FC` | `#070C16` |
| کاغذ | `#FFFFFF` | `#0C1422` |
| متن | `#0E1E33` | `#EAF0F8` |
| متن کم‌رنگ | `#5A78A0` | `#9DB8EC` |

قرمز `#B3261E` جدا از این پالت می‌ماند و **فقط** برای تعطیلات خرج می‌شود، در هر دو حالت روشن و تیره. در تقویم چاپی ایران قرمز معنای مشخصی دارد؛ همان معنا باید در اپ حفظ شود. مقادیر دقیق در `web/theme/theme.js`.

---

## ۱۱. امنیت و رمزنگاری

> **وضعیت: به تعویق افتاده.** تصمیم نهایی این است که فازهای اول روی راه‌اندازی و استفاده تمرکز کنند. این بخش و بخش‌های ۱۲ و ۱۳.۷ به‌عنوان مرجع آینده نگه داشته شده‌اند. آنچه امروز اجرا می‌شود در **بخش ۱۴** است.

Keycloak کنار گذاشته شد. برای اپ تک‌کاربره سه سطح ممکن است:

| سطح | چه چیزی را محافظت می‌کند | هزینه |
|---|---|---|
| ۰ — فقط VPN | دسترسی از اینترنت | صفر |
| ۱ — رمز عبور + دیسک رمز سرور | بکاپ یا دیسک لو رفته | نیم روز |
| ۲ — E2EE | سرور، بکاپ، و ارائه‌دهندهٔ میزبانی | یک تا دو روز |

**انتخاب: سطح ۲، از فاز ۲.** عقب‌گرد به E2EE یعنی مهاجرت همهٔ رکوردها و دور ریختن طراحی جست‌وجو.

### طرح کلید

```
رمز عبور ──Argon2id(salt, m=64MB, t=3, p=1)──▶ KEK
                                                │
DEK (۳۲ بایت تصادفی، یک‌بار ساخته می‌شود) ◀──unwrap──┘
   │
   └──AES-256-GCM──▶ رمزنگاری هر رکورد
```

دو لایه بودن مهم است: با تغییر رمز عبور فقط DEK دوباره wrap می‌شود، نه اینکه کل دیتابیس دوباره رمز شود.

در `meta` ذخیره می‌شود: `salt`، `wrappedDek`، `kdfParams`، `version`.
**هرگز** خود DEK یا رمز عبور ذخیره نشود. کلید با `extractable: false` وارد WebCrypto شود و فقط در حافظه بماند.

**recovery key:** یک رشتهٔ تصادفی ۲۵۶ بیتی به شکل base32 در راه‌اندازی اولیه تولید کن، DEK را با آن هم wrap کن و `wrappedDekRecovery` را نگه دار. کاربر باید تأیید کند که ذخیره‌اش کرده. بدون این، فراموشی رمز یعنی از دست رفتن کامل داده.

### رمزنگاری رکوردها

```ts
// هر نوشتن: IV تازهٔ ۹۶ بیتی. تکرار IV با همان کلید، GCM را کاملاً می‌شکند.
async function seal(dek: CryptoKey, data: unknown): Promise<Uint8Array> {
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const pt = new TextEncoder().encode(JSON.stringify(data));
  const ct = await crypto.subtle.encrypt({ name: 'AES-GCM', iv }, dek, pt);
  return new Uint8Array([...iv, ...new Uint8Array(ct)]);
}
```

اگر ترجیح می‌دهی نگران IV نباشی، `libsodium-wrappers` و XChaCha20-Poly1305 با nonce ۱۹۲ بیتی همین کار را بدون این ریسک می‌کند. عوضش حدود ۱۰۰ کیلوبایت WASM اضافه می‌شود و از شتاب سخت‌افزاری AES محروم می‌شوی. برای این حجم داده هر دو کافی‌اند.

Argon2id از `hash-wasm` (خود WebCrypto فقط PBKDF2 دارد که ضعیف‌تر است).
**روی موبایل حافظه را بالای ۶۴ مگابایت نبر** — گوشی‌های ضعیف و Safari در تب پس‌زمینه شکست می‌خورند.

### چه چیزی رمز می‌شود و چه چیزی نه

| رمز می‌شود | خام می‌ماند (برای کوئری لازم است) |
|---|---|
| `note.title`, `note.content`, `note.plain` | `note.id`, `note.dayKey`, `note.updatedAt`, `note.deletedAt` |
| `note.tags` | `task.done`, `task.order`, `task.dayKey` |
| `task.title` | `attachment.id`, `attachment.noteId`, `attachment.size` |
| `attachment.blob`, `attachment.name`, `attachment.mime` | |

**متادیتایی که ناچاراً لو می‌رود:** اینکه در چه روزهایی نوشته‌ای، چند کار داشته‌ای و چندتا را انجام داده‌ای، و حجم تقریبی هر یادداشت. برای دفترچهٔ شخصی این پذیرفتنی است. اگر نباشد باید `dayKey` را هم رمز کنی و آن‌وقت تقویم مجبور است همه‌چیز را رمزگشایی کند تا نقطه‌ها را نشان دهد — کند و بی‌ارزش.

### اثر روی جست‌وجو

FTS5 سمت سرور غیرممکن می‌شود. جست‌وجو کاملاً سمت کلاینت:

- در باز کردن قفل، `plain` همهٔ یادداشت‌ها رمزگشایی و در حافظه نگه داشته شود (چند هزار یادداشت ≈ چند مگابایت، مسئله‌ای نیست).
- برای جست‌وجوی فازی از `minisearch` یا `fuse.js` روی همان آرایه استفاده کن.
- اگر روزی از ده هزار یادداشت گذشتی، سراغ ایندکس معکوس رمزشده برو. آن روز خیلی دور است.

بخش ۵ فازبندی باید مطابق این بازنویسی شود.

### قفل و باز کردن قفل

- در هر بار بوت اپ، صفحهٔ ورود رمز. Argon2id حدود ۳۰۰ تا ۵۰۰ میلی‌ثانیه طول می‌کشد؛ یک اسپینر با متن روشن نشان بده.
- قفل خودکار بعد از بازهٔ قابل تنظیم (پیش‌فرض ۱۵ دقیقه بی‌فعالیتی) و روی `visibilitychange`.
- **بیومتریک:** با افزونهٔ `prf` در WebAuthn می‌شود DEK را به اثر انگشت گره زد. پشتیبانی مرورگرها هنوز ناهموار است؛ بگذار برای بعد و فعلاً رمز عبور بگیر.
- «مرا به خاطر بسپار» یعنی DEK را جایی روی دستگاه نگه داری، که کل امنیت در برابر دستگاه دزدیده‌شده را از بین می‌برد. اگر گذاشتی، صریح به کاربر بگو چه چیزی را دارد معامله می‌کند.

### احراز هویت سرور (فاز ۶)

چون سرور هیچ‌وقت متن خام نمی‌بیند، احراز هویت فقط برای جلوگیری از پر شدن دیسک و خواندن متادیتاست:

- یک توکن تصادفی بلند در راه‌اندازی دستگاه، در کوکی `HttpOnly` و `Secure`.
- rate limit روی اندپوینت‌ها، و سقف حجم آپلود.
- HTTPS اجباری، HSTS.
- هیچ رمز عبوری به سرور نرود. سرور اصلاً نمی‌داند رمز چیست.

### هزینهٔ عملکردی — عدد واقعی

| عملیات | زمان |
|---|---|
| باز کردن قفل (Argon2id) | ۳۰۰–۵۰۰ms، یک بار در هر نشست |
| رمزنگاری یادداشت ۵KB | زیر ۰٫۱ms |
| رمزگشایی ۱۰۰۰ یادداشت در بوت | حدود ۵۰–۱۵۰ms |
| رمزنگاری تصویر ۳۰۰KB | ۱–۳ms |

عملاً هیچ‌کدام محسوس نیستند. تنها چیزی که کاربر حس می‌کند، مکث یک‌بارهٔ باز کردن قفل است — و آن مکث عمدی است.

### دو چیزی که سخت‌تر می‌شود

1. **دیباگ.** دیگر نمی‌توانی دیتابیس را باز کنی و ببینی چه خبر است. یک ابزار توسعه بساز که با رمز، محتوای رکورد را نشان دهد.
2. **بکاپ.** فایل ZIP خروجی هم رمز است. اگر می‌خواهی بکاپ خام و خوانا داشته باشی، باید گزینهٔ «خروجی رمزگشایی‌شده» جدا بگذاری و با هشدار صریح.

---

## ۱۲. چنددستگاهی — گوشی، کامپیوتر شخصی، کامپیوتر محل کار

این نیاز سه چیز را عوض می‌کند: همگام‌سازی از فاز ۶ می‌آید به فاز ۴، «سطح ۰ — فقط VPN» حذف می‌شود، و مدل کلید باید چند keyring داشته باشد.

### ۱۲.۱ چرا رمزنگاری حالا اجباری است

سرور باید از شبکهٔ محل کار در دسترس باشد، و آن شبکه احتمالاً VPN خروجی را می‌بندد. پس سرور روی اینترنت عمومی می‌نشیند. E2EE دیگر احتیاط اضافی نیست، تنها چیزی است که بین یادداشت‌های تو و یک سرور در معرض اینترنت ایستاده.

### ۱۲.۲ چنددستگاهی چطور کار می‌کند (ساده است)

```
دستگاه جدید ──▶ GET /keys ──▶ { salt, wrappedDek, kdfParams }
                    │
       رمز عبور ────┴──Argon2id──▶ KEK ──unwrap──▶ DEK
```

هیچ تبادل کلیدی بین دستگاه‌ها لازم نیست. هر دستگاه مستقلاً از رمز عبور به همان DEK می‌رسد. افزودن دستگاه چهارم هم صفر هزینه دارد.

### ۱۲.۳ کامپیوتر محل کار — جدی‌ترین ریسک پروژه

روی ماشینی که مدیرش نیستی، رمز اصلی را تایپ می‌کنی. اگر MDM، عامل DLP یا افزونهٔ مرورگر سازمانی روی آن باشد، آن رمز قابل ثبت است. و چون یک DEK کل داده را باز می‌کند، لو رفتنش یعنی لو رفتن کل تاریخچه، نه فقط چیزی که سر کار باز کرده‌ای.

**راه‌حل: چند keyring به‌جای یک DEK.**

```ts
interface Keyring {
  id: string;            // 'work' | 'private' | ...
  label: string;
  wrappedDek: string;    // با KEK مشتق از رمزِ همان keyring
  salt: string;
  kdfParams: KdfParams;
}

interface Note {
  // …
  keyringId: string;     // هر یادداشت دقیقاً به یک keyring تعلق دارد
}
```

- keyring پیش‌فرض `work` روی هر سه دستگاه باز می‌شود.
- keyring `private` رمز جداگانه دارد و **هرگز روی دستگاه شرکت وارد نمی‌شود**. یادداشت‌هایش روی آن دستگاه به‌صورت قفل‌شده در فهرست دیده می‌شوند، بدون امکان باز شدن.
- انتخاب keyring هنگام ساخت یادداشت، و امکان جابه‌جایی بعدی (که یعنی رمزگشایی با یکی و رمزنگاری با دیگری).

اگر این را از فاز ۲ در مدل داده بگذاری تقریباً رایگان است. اضافه کردنش بعداً یعنی مهاجرت کل دیتابیس.

**عادت‌های تکمیلی روی دستگاه شرکت:** گزینهٔ «مرا به خاطر بسپار» را آنجا فعال نکن، قفل خودکار را روی دو دقیقه بگذار، و PWA را نصب نکن (فقط تب مرورگر، تا داده‌ای روی دیسک آن ماشین نماند). یک تنظیم «حالت دستگاه ناامن» بساز که این سه را یک‌جا اعمال کند.

جدای از بحث فنی: قرار گرفتن دادهٔ شخصی روی سخت‌افزار کارفرما می‌تواند مشمول سیاست بکاپ و بازرسی آن شرکت باشد. این را با شرایط کاری خودت بسنج.

### ۱۲.۴ تعارض ویرایش — مسئلهٔ تازه

سرور محتوا را نمی‌بیند، پس merge سمت سرور ممکن نیست. دو راه:

**الف) LWW با نسخهٔ متعارض — حدود یک روز کار**

هر رکورد `updatedAt` و `deviceId` دارد. اگر نسخهٔ ورودی و نسخهٔ محلی هر دو از آخرین همگام‌سازی مشترک جلوتر باشند، بازنده پاک نمی‌شود بلکه به‌عنوان یادداشت جدید با عنوان «نسخهٔ متعارض — {دستگاه} — {تاریخ}» ذخیره می‌شود و به کاربر اطلاع داده می‌شود.
ساده، قابل فهم، و هیچ‌وقت داده گم نمی‌کند. عوضش گاهی مجبوری دستی ادغام کنی.

**ب) Yjs (CRDT) — حدود سه تا چهار روز کار**

`@tiptap/extension-collaboration` + `y-prosemirror` رسمی‌اند. هر تغییر یک بلاب باینری است که **می‌شود رمزش کرد** و سرور فقط ذخیره و پخشش می‌کند بدون اینکه بفهمد چیست. ادغام واقعی و بدون تعارض، حتی وقتی دو دستگاه هفته‌ها آفلاین بوده‌اند.
هزینه: حجم داده رشد می‌کند و باید دوره‌ای فشرده شود، دیباگ سخت‌تر است، و بزرگ‌ترین بخش پیچیدگی کل پروژه است.

**پیشنهاد: از (الف) شروع کن.** با سه دستگاه و یک کاربر، ویرایش واقعاً هم‌زمان نادر است و «نسخهٔ متعارض» رفتار قابل قبولی است. اگر بعد از چند ماه دیدی مکرر اتفاق می‌افتد، (ب) را اضافه کن — مدل داده تغییر نمی‌کند چون محتوا در هر دو حالت یک بلاب رمزشده است.

برای کارها (`Task`) هرگز به Yjs نیاز نداری؛ LWW در سطح فیلد کافی است.

### ۱۲.۵ پروتکل همگام‌سازی

REST ساده، بدون WebSocket. یک کاربر و سه دستگاه، هیچ‌وقت به همگام‌سازی زنده نیاز ندارند.

```
GET  /sync?since={cursor}   →  رکوردهای تغییرکرده (ciphertext + متادیتای خام)
POST /sync                  →  تغییرات محلی + cursor جدید
```

- کِی: در باز شدن اپ، در `visibilitychange`، بعد از هر ذخیره با debounce سی ثانیه، و دستی با pull-to-refresh.
- صف تغییرات محلی در Dexie نگه داشته شود تا آفلاین بودن چیزی را نبازد.
- حذف نرم اجباری است (`deletedAt`)؛ بدون آن رکورد حذف‌شده از دستگاه دیگر برمی‌گردد.
- **ساعت دستگاه‌ها قابل اعتماد نیست.** برای مقایسه از شمارندهٔ سرور (`serverSeq`) استفاده کن، نه صرفاً `updatedAt` دستگاه.

### ۱۲.۶ ضمیمه‌ها در حالت چنددستگاهی

- آدرس‌دهی بر اساس هش محتوا (`sha256` قبل از رمزنگاری) تا فایل تکراری دوبار آپلود نشود.
- روی گوشی **تنبل** دانلود کن: فقط وقتی یادداشت باز شد. وگرنه اولین همگام‌سازی چند صد مگابایت از دیتای موبایلت را می‌خورد.
- سقف اندازهٔ فایل بگذار (مثلاً ۲۵ مگابایت) و در رابط کاربری صریح بگو.

### ۱۲.۷ فازبندی بازنگری‌شده

- **فاز ۰** — اسکلت
- **فاز ۱** — تقویم و تعطیلات
- **فاز ۲** — Dexie + کارها + PWA + **رمزنگاری و keyringها** + بکاپ دستی
- **فاز ۳** — ادیتور پایه
- **فاز ۴** — **سرور و همگام‌سازی** (منتقل‌شده از فاز ۶): SQLite، اندپوینت `/sync`، LWW با نسخهٔ متعارض، توکن دستگاه، Docker
- **فاز ۵** — محتوای غنی: تصویر، ضمیمه، کد، جدول (+ همگام‌سازی ضمیمه)
- **فاز ۶** — یادداشت آزاد، تگ، جست‌وجوی سمت کلاینت

از پایان فاز ۴ اپ روی هر سه دستگاه قابل استفاده است. فازهای ۵ و ۶ کیفیت زندگی‌اند، نه شرط کارکرد.

---

## ۱۳. همگام‌سازی زنده با رمزنگاری سرتاسری

نیاز روشن شد: تغییرات باید **بلادرنگ** روی هر سه دستگاه دیده شوند. این بخش، بند ۱۲.۴ را جایگزین می‌کند — گزینهٔ LWW کنار می‌رود و Yjs از همان ابتدا انتخاب می‌شود.

> تصمیم پیش‌نیاز: چون LWW محتوا را به‌صورت `JSONContent` ذخیره می‌کند و Yjs یک لاگ تغییرات، عقب‌گرد از یکی به دیگری یعنی مهاجرت کل داده. با نیاز به حالت زنده، از روز اول Yjs.

### ۱۳.۱ چرا رمزنگاری اینجا عملاً رایگان است

| عملیات | حجم | زمان |
|---|---|---|
| بستهٔ تغییر Yjs برای چند ثانیه تایپ | ۲۰–۲۰۰ بایت | رمزنگاری زیر ۰٫۱ms |
| رفت‌وبرگشت شبکه تا سرور | — | ۲۰–۱۵۰ms |
| رمزنگاری تصویر ۱ مگابایتی | ۱MB | ۲–۵ms |
| آپلود همان تصویر | ۱MB | صدها میلی‌ثانیه تا چند ثانیه |

سهم رمزنگاری از تأخیر محسوس، حدود یک هزارم است. عامل تعیین‌کنندهٔ سرعت، فاصلهٔ شبکه است نه کریپتو.

**در این پروژه سرور و هر سه دستگاه در آلمان‌اند:** RTT روی اتصال خانگی و اداری حدود ۵ تا ۲۰ میلی‌ثانیه و روی موبایل حدود ۲۰ تا ۴۰ میلی‌ثانیه. در این محدوده تایپ هم‌زمان روی دو دستگاه بومی حس می‌شود و بودجهٔ تأخیر اصلاً تنگ نیست.

نتیجهٔ عملی: **بسته‌های Yjs را دسته‌بندی یا تأخیرانداخته نکن.** هر تغییر بلافاصله فرستاده شود. الگوی batching فقط وقتی ارزش دارد که RTT از صد میلی‌ثانیه بگذرد.

### ۱۳.۲ سرور به‌عنوان رلهٔ کور

پروتکل استاندارد `y-websocket` روی سرور قابل استفاده نیست، چون برای محاسبهٔ diff باید state vector را بخواند. به‌جایش:

```
docs(docId, seq, ciphertext, deviceId, createdAt)   -- لاگ append-only
snapshots(docId, seq, ciphertext, createdAt)        -- تصویر فشرده‌شده
blobs(hash, ciphertext, size, createdAt)            -- ضمیمه‌ها
```

- **WebSocket** فقط برای پخش: کلاینت بسته را رمز می‌کند و می‌فرستد، سرور `seq` می‌دهد، ذخیره می‌کند، و به سایر اتصال‌های همان `docId` پخش می‌کند.
- **اتصال تازه:** آخرین snapshot + همهٔ بسته‌های بعد از آن را می‌گیرد، رمزگشایی و `Y.applyUpdate` می‌کند.
- چون تغییرات Yjs **جابه‌جایی‌پذیر و idempotent** هستند، ترتیب رسیدن مهم نیست و سرور لازم نیست چیزی بفهمد.
- **فشرده‌سازی لاگ:** وقتی تعداد بسته‌های یک سند از حدود ۵۰۰ گذشت، کلاینت دسکتاپ در حالت بی‌کاری همه را رمزگشایی می‌کند، با `Y.encodeStateAsUpdate` یک بستهٔ واحد می‌سازد، رمزش می‌کند و به‌عنوان snapshot می‌فرستد؛ سرور بسته‌های قدیمی‌تر را حذف می‌کند. بدون این، لاگ بی‌نهایت رشد می‌کند و بوت سرد کند می‌شود.

### ۱۳.۳ دانه‌بندی اسناد

**یک `Y.Doc` برای هر روز:**

```ts
ydoc.getXmlFragment('note')   // محتوای TipTap همان روز
ydoc.getArray('tasks')        // هر عضو یک Y.Map: {id,title,done,order,priority}
```

مزیت: تیک زدن یک کار روی گوشی، همان لحظه روی دسکتاپ دیده می‌شود، بدون کانال همگام‌سازی دوم. متن و کارهای یک روز اتمیک با هم همگام می‌شوند.

یادداشت‌های آزاد هر کدام سند مستقل خودشان را دارند (`docId = note:{id}`).
متادیتای فهرست‌ها (عنوان، تگ، `updatedAt`) در یک سند سبک جداگانه به‌نام `index` نگه داشته شود تا نمای فهرست بدون باز کردن همهٔ اسناد کار کند.

### ۱۳.۴ ضمیمه‌ها در حالت زنده

جریان دقیق سناریوی «عکس از گوشی، توضیح از دسکتاپ»:

1. گوشی عکس را فشرده می‌کند (حداکثر ۱۶۰۰px، WebP) و `sha256` را قبل از رمزنگاری حساب می‌کند.
2. رمز می‌کند و به `POST /blobs` می‌فرستد.
3. **بلافاصله** — بدون منتظر ماندن آپلود — یک نود در سند Yjs درج می‌کند با `{attachmentId, hash, width, height, status:'uploading'}`. این بسته چند ده بایت است.
4. دسکتاپ در همان لحظه نود را می‌بیند و یک placeholder با ابعاد درست نشان می‌دهد. **کاربر همان‌جا شروع به نوشتن توضیح می‌کند.**
5. وقتی آپلود تمام شد، گوشی `status` را به `'ready'` تغییر می‌دهد؛ دسکتاپ فایل را دانلود، رمزگشایی و نمایش می‌دهد.

`width` و `height` را حتماً در همان نود بگذار، وگرنه با رسیدن تصویر، چیدمان می‌پرد و مکان‌نمای نوشتن جابه‌جا می‌شود.

روی گوشی دانلود ضمیمه‌ها تنبل باشد — فقط یادداشتی که باز شده.

### ۱۳.۵ سه چیزی که واقعاً کند می‌شوند

1. **بوت سرد.** رمزگشایی و اعمال لاگ طولانی. راه‌حل: فشرده‌سازی snapshot (بند ۱۳.۲) + کش محلی وضعیت.
2. **کش محلی Yjs.** پرووایدر آمادهٔ `y-indexeddb` وضعیت را **خام** ذخیره می‌کند. برای حفظ رمزنگاری در حالت سکون باید یک پرووایدر ساده بنویسی که snapshot را رمزشده نگه دارد و در باز کردن قفل، رمزگشایی کند. حدود صد خط کد. اگر به رمزنگاری دیسک هر سه دستگاه اتکا کنی می‌توانی از `y-indexeddb` استفاده کنی، ولی آن‌وقت یادداشت‌هایت روی گوشی در حالت باز، خام روی دیسک‌اند.
3. **WebSocket پایدار روی موبایل.** باتری می‌خورد و iOS در پس‌زمینه اتصال را می‌بندد. راه‌حل: اتصال فقط در پیش‌زمینه، قطع در `visibilitychange`، و همگام‌سازی کامل هنگام بازگشت. «زنده» یعنی زنده وقتی نگاه می‌کنی — که دقیقاً همان چیزی است که می‌خواهی.

### ۱۳.۶ کتابخانه‌ها

```bash
yarn add yjs @tiptap/extension-collaboration y-protocols
# سمت سرور: هیچ‌کدام — بک‌اند FastAPI است و بستهٔ Yjs را باز نمی‌کند (بخش ۱۶)
```

`@tiptap/extension-collaboration-cursor` را فعلاً نیاز نداری (نشانگر کاربران دیگر برای یک کاربر بی‌معناست)، ولی یک نشانگر ساده «این یادداشت روی دستگاه دیگری باز است» با `awareness` ارزش دارد و رایگان است. دادهٔ awareness هم رمز شود؛ گرچه ناپایدار است، همان‌قدر افشاگر است.

### ۱۳.۷ اثر بر keyring

با توضیح تو دربارهٔ کامپیوتر محل کار (دیسک رمزنگاری‌شده، بدون عامل مانیتورینگ، تحت مدیریت خودت)، بند ۱۲.۳ دیگر ضرورت ندارد.

**فیلد `keyringId` را در مدل داده نگه دار ولی فقط یک keyring بساز.** هزینه‌اش امروز صفر است و اگر روزی شرایط عوض شد — دستگاه جدید، همکار، لپ‌تاپ شرکتی دیگر — بدون مهاجرت داده اضافه می‌شود.

### ۱۳.۸ فازبندی نهایی

- **فاز ۰** — اسکلت
- **فاز ۱** — تقویم و تعطیلات
- **فاز ۲** — Dexie + کارها + PWA + رمزنگاری (DEK، Argon2id، recovery key) + بکاپ دستی
- **فاز ۳** — ادیتور TipTap با Yjs از همان ابتدا، ولی تک‌دستگاهه (پرووایدر محلی رمزشده)
- **فاز ۴** — سرور رلهٔ کور: WebSocket، لاگ رمزشده، snapshot، توکن دستگاه، Docker. **از اینجا اپ واقعاً چنددستگاهی و زنده است.**
- **فاز ۵** — محتوای غنی: تصویر، ضمیمه، کد، جدول + همگام‌سازی بلاب
- **فاز ۶** — یادداشت آزاد، تگ، جست‌وجوی سمت کلاینت

فاز ۴ بزرگ‌ترین بخش پروژه است، حدود چهار تا شش روز کار متمرکز. بقیهٔ فازها هرکدام یک تا دو روز.

---

## ۱۴. تصمیم نهایی برای اجرا — سادگی اول

رمزنگاری سرتاسری به تعویق افتاد. اپ کاملاً شخصی است و هدف فازهای اول، **رسیدن به یک ابزار قابل استفادهٔ روزانه** است. این بخش بر بخش‌های ۱۱، ۱۲ و ۱۳.۷ اولویت دارد.

### ۱۴.۱ قاعدهٔ طلایی (بازنگری‌شده در فاز ۵): ورود دروازهٔ کل اپ است

> **بازنگری ۱۴۰۵/۰۶/۰۲ (فاز ۵):** نسخهٔ اول این بند («ورود دروازهٔ همگام‌سازی
> است، نه دروازهٔ اپ» — اپ همیشه با دادهٔ محلی باز شود، حتی بدون نشست) آگاهانه
> کنار گذاشته شد. چون این اپ فقط برای یک کاربر است، کاربر ترجیح داد پیش از
> ورود **هیچ صفحه‌ای** — نه تقویم، نه کارها، نه یادداشت‌ها — نمایش داده نشود.
> استدلال آفلاین‌بودنِ بند ۱۵.۳ («PWA آفلاین بی‌فایده می‌شود») همچنان درست است،
> ولی برای این پروژهٔ تک‌کاربره وزنش کمتر از حریم خصوصی کامل قبل از ورود بود.

```
باز شدن اپ ──▶ نشست معتبر؟
                  ├─ بله ──▶ اپ کامل رندر می‌شود، اتصال WebSocket برقرار می‌شود
                  └─ خیر ──▶ فقط صفحهٔ ورود (رمز → TOTP/کد بازیابی)
```

پیاده‌سازی: `SessionProvider` در `app/layout.js` وضعیت نشست را نگه می‌دارد؛
`AuthGate` (همان‌جا، دور کل `children}`) قبل از نشست معتبر چیزی جز فرم ورود
رندر نمی‌کند — نه فقط صفحهٔ تنظیمات، **کل درخت اپ**.

نوشتن هنوز هیچ‌وقت داده گم نمی‌کند — منتها این تضمین از «مرحلهٔ ورود» شروع
می‌شود، نه از باز شدن اپ. تا وقتی وارد نشوی، چیزی برای نوشتن هم نمی‌بینی.

### ۱۴.۲ احراز هویت

**رمز عبور**

- هش با Argon2id سمت سرور (`@node-rs/argon2`). پارامترها روی سرور می‌توانند سخاوتمند باشند: `m=64MB, t=3, p=1`.
- کاربر تکی است؛ یک ردیف در جدول `user`. ثبت‌نام عمومی وجود ندارد — کاربر اولیه با یک اسکریپت CLI ساخته می‌شود.

**TOTP (Google Authenticator)**

```bash
yarn add otpauth qrcode
```

- **پارامترها را دستکاری نکن:** `SHA1`، ۶ رقم، دورهٔ ۳۰ ثانیه. Google Authenticator فقط همین‌ها را می‌فهمد و اگر SHA-256 یا ۸ رقم بدهی، بی‌صدا کد اشتباه تولید می‌کند.
- URI ثبت:
  `otpauth://totp/Daftar:ali?secret={BASE32}&issuer=Daftar&algorithm=SHA1&digits=6&period=30`
- پنجرهٔ پذیرش `±1` (یعنی ۳۰ ثانیه اختلاف ساعت مجاز).
- **جلوگیری از replay:** آخرین counter مصرف‌شده را ذخیره کن و همان کد را دوباره نپذیر.
- `totpSecret` را رمزشده در دیتابیس نگه دار (یک کلید ثابت در متغیر محیطی کافی است) — نه خام.

**کدهای بازیابی — این را حذف نکن**

ده کد یک‌بارمصرف در راه‌اندازی تولید کن، هش‌شده ذخیره کن، و یک بار به کاربر نشان بده. اگر گوشی‌ات گم شود و این‌ها را نداشته باشی، تنها راه، دسترسی مستقیم به دیتابیس روی سرور است.

**نشست**

- کوکی امضاشده، `HttpOnly`, `Secure`, `SameSite=Lax`.
- عمر بلند (۳۰ روز) با تمدید غلتان. روی PWA موبایل، لاگین مکرر کشنده است.
- نشست‌ها در جدول `sessions` تا بتوانی از تنظیمات، دستگاه‌ها را ببینی و باطل کنی.
- **WebSocket:** اگر هم‌مبدأ باشد، کوکی در handshake ارسال می‌شود و کافی است. اتصال بدون نشست معتبر باید رد شود، نه اینکه بی‌صدا وصل بماند.

**محدودسازی نرخ**

- حداکثر ۵ تلاش ناموفق ورود در ۱۵ دقیقه از هر IP.
- برای مرحلهٔ TOTP جداگانه بشمار. کد شش‌رقمی فقط یک میلیون حالت دارد؛ بدون محدودسازی، brute-force عملی است.

### ۱۴.۳ چه چیزهایی حذف می‌شوند

از طرح قبلی این‌ها فعلاً کنار می‌روند:

- Argon2id سمت کلاینت، DEK، `wrappedDek`، recovery key رمزنگاری
- صفحهٔ باز کردن قفل و قفل خودکار
- `keyringId` — **حذفش کن.** با نبود رمزنگاری به چیزی ارجاع نمی‌دهد. افزودن یک ستون در آینده ده دقیقه کار است.
- پرووایدر محلی رمزشدهٔ Yjs → از `y-indexeddb` استاندارد استفاده کن (بند ۱۳.۵ مورد ۲ منتفی است)

آنچه **می‌ماند** و حیاتی است:

- HTTPS اجباری روی سرور
- بکاپ خودکار روزانه از فایل SQLite و پوشهٔ ضمیمه‌ها
- خروجی ZIP دستی از داخل اپ (فاز ۲)

### ۱۴.۴ سه قاعده که افزودن رمزنگاری در آینده را ارزان نگه می‌دارد

اگر روزی نظرت عوض شد، رعایت این سه یعنی حدود یکی دو روز کار و **صفر مهاجرت داده**:

1. **سرور بسته‌های Yjs را به‌عنوان `BLOB` مبهم ذخیره کند** و هیچ‌وقت parse نکند. حتی برای دیباگ.
2. **جست‌وجوی تمام‌متن سمت کلاینت بماند.** وسوسه‌شدن به FTS5 روی متن یادداشت‌ها، بزرگ‌ترین دام است — بعداً باید کل قابلیت را دور بریزی.
3. **هیچ قابلیتی که سرور برای اجرایش باید محتوا را بخواند نساز:** رندر سمت سرور، تولید PDF روی سرور، خلاصهٔ ایمیلی، پیش‌نمایش لینک.

اگر این سه رعایت شوند، رمزنگاری بعداً فقط یک لایهٔ بسته‌بندی روی مرز شبکه است، نه بازنویسی معماری.

### ۱۴.۵ فازبندی نهایی

جایگزین بند ۱۳.۸.

- **فاز ۰** — اسکلت: Next.js، MUI، RTL، وزیرمتن، تم، ناوبری
- **فاز ۱** — تقویم شمسی و تعطیلات
- **فاز ۲** — Dexie، کارها، PWA، خروجی ZIP دستی
- **فاز ۳** — ادیتور TipTap با Yjs و `y-indexeddb` (هنوز تک‌دستگاهه)
- **فاز ۴** — سرور: SQLite، ورود با رمز و TOTP، رلهٔ WebSocket، snapshot، Docker. **از اینجا اپ چنددستگاهی و زنده است.**
- **فاز ۵** — تصویر، ضمیمه، بلوک کد، جدول + همگام‌سازی بلاب
- **فاز ۶** — یادداشت آزاد، تگ، جست‌وجوی سمت کلاینت
- **فاز ۷ (اختیاری، بعداً)** — رمزنگاری سرتاسری طبق بخش‌های ۱۱ و ۱۳

با حذف رمزنگاری از مسیر اصلی، فاز ۲ و فاز ۴ هرکدام حدود یک روز سبک‌تر می‌شوند.

---

## ۱۵. کنار گذاشتن آفلاین — دامنهٔ دقیق

> **تصحیح یک فرض:** نگرانی «آفلاین روی دو دستگاه یعنی تعارض» مربوط به مدل last-write-wins است، نه Yjs. CRDT دقیقاً برای همین ساخته شده: دو دستگاه که هفته‌ها آفلاین ویرایش کرده‌اند، هنگام اتصال بدون از دست رفتن داده و بدون پرسش از کاربر ادغام می‌شوند. **Yjs را نگه دار.**

دلیل واقعی کنار گذاشتن آفلاین این است که یک دستهٔ کاری کامل اضافه می‌کند بدون آنکه قابلیت جدیدی بدهد.

### ۱۵.۱ چه چیزی حذف می‌شود

- Service Worker و استراتژی کش (`@serwist/next`)
- پوستهٔ آفلاین و صفحهٔ fallback
- جریان نصب PWA و راهنمای «افزودن به صفحهٔ اصلی»
- `navigator.storage.persist()` و نمایش مصرف فضا
- حالت‌های رابط کاربری «آفلاین · در صف»
- بند ۶ (ماندگاری داده روی موبایل) به‌طور کامل

### ۱۵.۲ چه چیزی می‌ماند و چرا ارزان است

**`y-indexeddb` را نگه دار.** یک خط کد است و سه چیز می‌دهد:

```ts
new IndexeddbPersistence(docId, ydoc);   // همین
```

1. از **فاز ۲** یک اپ کاملاً کارآمد روی یک دستگاه داری، بدون هیچ سروری.
2. بارگذاری آنی است؛ باز شدن اپ منتظر رفت‌وبرگشت شبکه نمی‌ماند.
3. قطعی موقت وسط کار داده را نمی‌برد؛ وقتی اتصال برگردد خودش همگام می‌شود.

**Dexie دیگر لازم نیست.** با Yjs به‌عنوان منبع حقیقت و `y-indexeddb` به‌عنوان لایهٔ ماندگاری، یک لایهٔ ذخیره‌سازی کمتر داری. فقط ضمیمه‌های باینری به یک `IndexedDB` سادهٔ جداگانه نیاز دارند (فاز ۶).

**نکتهٔ کلیدی:** افزودن سرور بعداً هیچ کدی در رابط کاربری تغییر نمی‌دهد. فقط یک پرووایدر دوم کنار پرووایدر محلی می‌نشیند:

```ts
new IndexeddbPersistence(docId, ydoc);        // فاز ۲
new WebsocketProvider(url, docId, ydoc);      // فاز ۵ — اضافه می‌شود، جایگزین نمی‌شود
```

هیچ کد دورریختنی نوشته نمی‌شود.

### ۱۵.۳ هزینه‌ای که می‌پذیری

بدون Service Worker، اپ **بدون اینترنت اصلاً باز نمی‌شود** — چون فایل‌های اپ از سرور می‌آیند. دادهٔ محلی سالم است ولی به آن دسترسی نداری.

با توجه به اینکه گوشی ابزار اصلی است و روزانه می‌نویسی، این در مترو، آسانسور و قطار خودش را نشان می‌دهد. اگر آزاردهنده شد، فاز آفلاین را جلو بیاور: چون Yjs از قبل سر جایش است، آن فاز تقریباً فقط کار Service Worker است و نه منطق ادغام — حدود یک روز، نه سه روز.

### ۱۵.۴ فازبندی نهایی

جایگزین بند ۱۴.۵.

- **فاز ۰** — اسکلت: Next.js، MUI، RTL، وزیرمتن، تم، ناوبری
- **فاز ۱** — تقویم شمسی و تعطیلات
- **فاز ۲** — Yjs + `y-indexeddb` + کارهای روزانه → **اپ روی یک دستگاه قابل استفاده است**
- **فاز ۳** — ادیتور TipTap روی همان سند روزانه
- **فاز ۴** — سرور: SQLite، ورود با رمز و TOTP، نشست
- **فاز ۵** — رلهٔ WebSocket + snapshot → **چنددستگاهی و زنده**
- **فاز ۶** — تصویر، ضمیمه، بلوک کد، جدول
- **فاز ۷** — یادداشت آزاد، تگ، جست‌وجوی سمت کلاینت
- **فاز ۸ (اختیاری)** — آفلاین و PWA
- **فاز ۹ (اختیاری)** — رمزنگاری سرتاسری (بخش‌های ۱۱ و ۱۳)

فاز ۴ و ۵ عمداً جدا شده‌اند: سرور و احراز هویت یک تکه است، همگام‌سازی زنده تکهٔ دیگر. اگر وسط کار خسته شدی، پایان فاز ۳ یک ابزار واقعی و روزانه در دست داری.

### ۱۵.۵ Yjs چه چیزی را حل می‌کند و چه چیزی را نه

**حل می‌کند:** هیچ داده‌ای گم نمی‌شود، هر دو دستگاه به وضعیت یکسان همگرا می‌شوند، ترتیب رسیدن بسته‌ها بی‌اهمیت است، و هفته‌ها آفلاین بودن مشکلی ایجاد نمی‌کند. سخت‌ترین بخش مسئله همین است.

**حل نمی‌کند — این چهار مورد را خودت باید بنویسی:**

1. **صف آپلود ضمیمه.** فایل‌های باینری بیرون از CRDT هستند. نود تصویر فوراً همگام می‌شود ولی خود فایل نه. لازم است:
   - فیلد `status: 'pending' | 'uploading' | 'ready'` روی نود
   - صف آپلود مستقل که با بازگشت اتصال ادامه می‌دهد
   - نمایش placeholder با ابعاد درست روی دستگاه مقابل تا رسیدن فایل
2. **همگرایی ≠ درستی معنایی.** ویرایش هم‌زمان یک جمله روی دو دستگاه، در سطح کاراکتر ادغام می‌شود و ممکن است متن درهم‌آمیخته بدهد. داده گم نمی‌شود ولی نیاز به بازخوانی دارد. برای تک‌کاربر نادر است.
3. **حذف سند.** مسئلهٔ کلاسیک CRDT. حذف باید در یک سند `index` جداگانه به‌صورت tombstone مدیریت شود، نه با پاک کردن سند. وگرنه یادداشتِ حذف‌شده با محتوای دستگاه دیگر برمی‌گردد.
4. **پوستهٔ اپ.** Yjs فقط دادهٔ داخل اپ است. باز شدن اپ بدون اینترنت به Service Worker نیاز دارد و ربطی به CRDT ندارد.

**برآورد بازنگری‌شدهٔ فاز ۸ (آفلاین): یک تا دو روز**، نه یک روز. Service Worker ساده است؛ صف ضمیمه و معنای حذف کار واقعی دارند.

---

## ۱۶. بک‌اند

### ۱۶.۱ بک‌اند واقعاً چه می‌کند

| مسئولیت | توضیح |
|---|---|
| احراز هویت | ورود با رمز، تأیید TOTP، نشست |
| رلهٔ Yjs | دریافت بسته، تخصیص `seq`، ذخیره به‌عنوان `BLOB`، پخش به اتصال‌های دیگرِ همان `docId` |
| snapshot | پذیرش تصویر فشرده‌شده از کلاینت، حذف بسته‌های قدیمی‌تر |
| ضمیمه | آپلود و دانلود فایل، آدرس‌دهی با هش |

**و کاری که نمی‌کند:** هرگز بستهٔ Yjs را باز نمی‌کند، محتوا را نمی‌فهمد، جست‌وجو نمی‌کند، رندر نمی‌کند. (قاعده‌های بند ۱۴.۴)

نتیجه: **سرور به هیچ کتابخانهٔ Yjs نیاز ندارد.** استدلال «Yjs در اکوسیستم Node است پس بک‌اند Node باشد» اینجا اعتبار ندارد. حجم تخمینی کل بک‌اند: زیر ۱۰۰۰ خط. بار: یک کاربر، سه اتصال.

### ۱۶.۲ انتخاب: FastAPI

نه به دلیل برتری فنی — Node هم کاملاً درست است — بلکه به این دلایل:

- سال‌ها تجربهٔ عملی با آن؛ سریع‌ترین مسیر به فاز ۵
- WebSocket در FastAPI اول‌شخص و ساده است
- همان چیدمان آشنا: nginx جلو، Next.js روی یک پورت، FastAPI روی پورت دیگر، Docker Compose

**استدلال مقابل (منصفانه):** با Node یک زبان و تایپ مشترک بین کلاینت و سرور داری. ولی پروتکل سیم فقط `{docId, seq, payload}` است؛ اشتراک تایپ عملاً چیزی نمی‌خرد.

```
fastapi, uvicorn          # سرور و WebSocket
argon2-cffi               # هش رمز عبور
pyotp                     # TOTP سازگار با Google Authenticator
qrcode[pil]               # QR ثبت اولیه
python-multipart          # آپلود ضمیمه
```

پایگاه داده: `sqlite3` استاندارد با `PRAGMA journal_mode=WAL`. در این ابعاد ORM لازم نیست و فقط لایه اضافه می‌کند.

### ۱۶.۳ دو تلهٔ عملی

1. **WebSocket را داخل Next.js نگذار.** Route Handlerهای App Router از upgrade پشتیبانی نمی‌کنند؛ مجبور می‌شوی custom server بنویسی و بخشی از بهینه‌سازی‌های Next را از دست بدهی. سرویس جدا این را کلاً منتفی می‌کند.
2. **یک worker، نه بیشتر.** پخش پیام از یک دیکشنری در حافظه انجام می‌شود:
   ```python
   rooms: dict[str, set[WebSocket]] = {}
   ```
   با چند worker در uvicorn، دستگاه‌های وصل‌شده به workerهای مختلف همدیگر را نمی‌بینند و باگ کاملاً گیج‌کننده‌ای می‌سازد. برای یک کاربر، یک worker از هر نظر کافی است. در `docker-compose.yml` صریح بنویس `--workers 1` و یک کامنت بگذار که چرا.

### ۱۶.۴ طرح جدول‌ها

```sql
PRAGMA journal_mode = WAL;

CREATE TABLE user (
  id INTEGER PRIMARY KEY, username TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL, totp_secret TEXT NOT NULL,
  totp_last_counter INTEGER DEFAULT 0, created_at TEXT NOT NULL
);

CREATE TABLE recovery_code (
  id INTEGER PRIMARY KEY, code_hash TEXT NOT NULL, used_at TEXT
);

CREATE TABLE session (
  token_hash TEXT PRIMARY KEY, device_label TEXT,
  created_at TEXT NOT NULL, last_seen_at TEXT NOT NULL, expires_at TEXT NOT NULL
);

CREATE TABLE doc_update (
  doc_id TEXT NOT NULL, seq INTEGER NOT NULL,
  payload BLOB NOT NULL,              -- مبهم، هرگز parse نمی‌شود
  device_id TEXT, created_at TEXT NOT NULL,
  PRIMARY KEY (doc_id, seq)
);

CREATE TABLE doc_snapshot (
  doc_id TEXT PRIMARY KEY, seq INTEGER NOT NULL,
  payload BLOB NOT NULL, created_at TEXT NOT NULL
);

CREATE TABLE blob (
  hash TEXT PRIMARY KEY, size INTEGER NOT NULL,
  mime TEXT, created_at TEXT NOT NULL   -- خود فایل روی دیسک، نه در دیتابیس
);

CREATE TABLE login_attempt (
  ip TEXT NOT NULL, at TEXT NOT NULL, ok INTEGER NOT NULL
);
```

`seq` با `MAX(seq)+1` در یک تراکنش تخصیص داده شود. ضمیمه‌ها روی دیسک در `data/blobs/{hash[:2]}/{hash}` ذخیره شوند، نه داخل SQLite.

### ۱۶.۵ اندپوینت‌ها

```
POST   /auth/login          {username, password}        → نیاز به TOTP
POST   /auth/totp           {code}                      → کوکی نشست
POST   /auth/recovery       {code}                      → کوکی نشست
POST   /auth/logout
GET    /auth/me                                         → وضعیت نشست

WS     /sync/{doc_id}        دوطرفه، بستهٔ باینری
GET    /sync/{doc_id}/since?seq=N                        → بازیابی بعد از قطعی
POST   /sync/{doc_id}/snapshot                           → فشرده‌سازی

POST   /blobs                multipart                   → {hash}
GET    /blobs/{hash}
```

همهٔ مسیرها جز `/auth/*` نیازمند نشست معتبرند. اتصال WebSocket بدون نشست باید **رد** شود، نه اینکه بی‌صدا باز بماند.

### ۱۶.۶ استقرار

```
nginx ──┬── /            → Next.js (پورت ۳۰۰۰)
        └── /api, /sync  → FastAPI (پورت ۸۰۰۰، تک worker)
```

`proxy_read_timeout` را برای مسیر `/sync` بالا ببر (مثلاً ۳۶۰۰ ثانیه) وگرنه nginx اتصال WebSocket بی‌کار را می‌بندد و کلاینت مدام وصل و قطع می‌شود. هدرهای `Upgrade` و `Connection` را هم فراموش نکن.

بکاپ: یک cron روزانه که با `sqlite3 .backup` از دیتابیس کپی می‌گیرد (نه `cp` روی فایل باز) و پوشهٔ `blobs` را آرشیو می‌کند.
